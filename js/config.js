// hyuk.xyz Supabase config
// Fill these in from your Supabase project (Settings → API).
// Anon key is safe to expose in browser code; row-level security policies
// (defined in supabase/schema.sql) protect writes.
window.SUPABASE_CONFIG = {
	url: '',
	anonKey: ''
};
